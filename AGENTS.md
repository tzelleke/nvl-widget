# Coding Guidelines

## Python

- Follow PEP8 style conventions
- Use Google-style docstrings for all public functions, methods, and classes
- When importing multiple names from a module, use braces with each name on its own line:

  ```python
  from module import (
      FirstName,
      SecondName,
      ThirdName,
  )
  ```
